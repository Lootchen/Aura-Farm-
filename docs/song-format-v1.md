# Aura Farm Song Package v1

Aura Farm treats song timing, audio source and gameplay charts as one validated content package.

## Registry

`songs/index.json` is the catalog consumed by the game and Chart Lab.

```json
{
  "schemaVersion": 1,
  "defaultSong": "glasshouse-circuit",
  "songs": [
    {
      "id": "glasshouse-circuit",
      "file": "glasshouse-circuit.json"
    }
  ]
}
```

## Timing

Song Package v1 uses constant BPM.

```json
"timing": {
  "mode": "constant",
  "bpm": 110,
  "beats": 68,
  "beatsPerBar": 4,
  "stepsPerBeat": 2,
  "countInBeats": 4,
  "offsetMs": 0
}
```

`offsetMs` means the audio time where musical beat zero occurs. For file-backed songs a positive offset means beat zero is that many milliseconds after the beginning of the file.

`stepsPerBeat` defines the legal chart grid.

## Audio modes

### Procedural

```json
"audio": {
  "mode": "procedural",
  "engine": "aura-procedural-v1",
  "stems": [
    "drums",
    "bass",
    "harmony",
    "lead",
    "aura",
    "boss"
  ]
}
```

Procedural songs require `composition` and use `offsetMs: 0`.

The loader can reconstruct the exact musical frame at each beat, so `music.stem` is verified semantically.

### File

```json
"audio": {
  "mode": "file",
  "src": "../assets/audio/my-track.ogg",
  "gain": 0.9,
  "stems": ["mix"]
}
```

`src` is resolved relative to the song package JSON.

The game decodes the asset with Web Audio and checks that its duration covers:

```text
offsetMs / 1000 + beats * 60 / bpm
```

A file song may include analysis generated by Chart Lab:

```json
"analysis": {
  "version": 1,
  "type": "mix-energy",
  "windowMs": 84,
  "durationSeconds": 92.4,
  "stepEnergy": [0.1, 0.24, 0.05],
  "minEventEnergy": 0.018
}
```

`stepEnergy` contains normalized RMS energy on the legal rhythmic grid.

This proves that authored events land on audible parts of the mix. It does **not** identify individual instruments inside a stereo mix.

## Composition

`composition` is mandatory for procedural songs and optional/ignored for file playback.

It contains:

- drum patterns;
- bass patterns;
- lead patterns;
- Aura patterns;
- bars/sections.

The validator checks pattern lengths, bar references and total song length.

## Charts

A song may contain multiple charts:

```json
{
  "id": "standard",
  "name": "Standard",
  "difficulty": "standard",
  "mechanicsVersion": "aura-chart-v1",
  "events": []
}
```

v1 mechanics:

- `tap`;
- `slide` with `mode: "trace"`.

Each event declares music metadata:

- `music.stem`;
- `music.intent`;
- `music.energy`;
- `music.phrase`;
- optional `music.contour`.

For file-backed songs using `stems: ["mix"]`, TRACE should normally use authored anchors and `contour: false`.

## Validation strength

### semantic-stem

Procedural only. The engine knows which stem is audible on every step.

### mix-energy

File audio with Chart Lab analysis. The engine verifies grid + audible mix energy.

### grid-only

File audio without analysis. Structural/timing validation passes, but the package is explicitly marked as not audio-verified.

## External audio production workflow

1. obtain/create a track with commercial redistribution rights;
2. add the file under `assets/audio/`;
3. record its license in `assets/audio/LICENSES.md`;
4. create/import a file-mode song package in Chart Lab;
5. set BPM, total beats and offset;
6. load/analyze the waveform;
7. author Tap/TRACE against the real audio;
8. resolve validation errors;
9. export JSON into `songs/`;
10. register it in `songs/index.json`.

Use `docs/file-song-template.json` as a starting point.
